#include <stdio.h>

int main()
{
   // convert the length from cm into meters and kilo meters
    int l;
    printf("enter the length::");
    scanf("%d",&l);
    printf("length in meters ::%d",l/100);
    printf("length in kilo meters::%d ",1/100000);
    return 0;
}
///=======================================================================================================//
